const styles = {
  modal: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
  closeButton: {
    cursor: "pointer",
    color: "#536686f2",
  } as React.CSSProperties,
};

export default styles;
