import { color } from "@styles/colors";

const styles = {
  button: {
    color: color("SystemLabel1"),
    fontSize: 10,
  } as React.CSSProperties,
  buttonSelected: {
    color: color("SystemLabel1"),
    fontSize: 10,
    backgroundColor: "#b0b0b026",
  } as React.CSSProperties,
};

export default styles;
