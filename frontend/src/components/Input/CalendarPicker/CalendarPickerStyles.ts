const styles = {
  paperStyle: {
    zIndex: 1500,
    display: "flex",
    alignContent: "center",
    justifyContent: "center",
  } as React.CSSProperties,
  contentStyle: {
    p: 1.5,
    fontSize: 13,
    display: "flex",
    flexDirection: "column",
    alignContent: "center",
    justifyContent: "center",
  } as React.CSSProperties,
};

export default styles;
